import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingHorizontal: 12,
    paddingTop: 40,
    backgroundColor: "rgba(219, 189, 189, 0.2)",
  },
  message: {
    marginBottom: 10,
    color: "purple",
    fontSize: 18,
    textAlign: "center",
  },
  smile: {
    fontSize: 56,
    textAlign: "center",
  },
});